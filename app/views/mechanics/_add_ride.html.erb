<%= form_with url:(mechanic_ride_path(mechanic.id)), method: :get, local: true do |f| %>
  <%= f.label "Add a ride to workload: " %>
  <%= f.number_field :search, placeholder: "Enter Rides ID" %>

  <%= f.submit "Submit" %>
<% end %>
